extends layout.pug

block content
    h1 Delete Book
    p Are you sure you want to delete this book?
    form(action="/books/delete" method="POST")
        input(type="hidden" name="book_id" value=book_id)
        input(type="submit" value="Yes, Delete Book")
    a(href="/books") No, Cancel